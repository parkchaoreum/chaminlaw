# 검색 노출 설정 안내

`https://www.chaminlaw.com` 으로 사람들이 검색해서 들어올 수 있게 하는 순서입니다.

**중요:** 아래 1~2번을 끝내야 3번부터가 의미가 있습니다. 사이트가 인터넷에
올라가 있지 않으면 검색엔진에 등록할 대상 자체가 없습니다.

소요 시간은 **작업 자체는 1~2시간**, 실제 검색 결과에 뜨기까지는
**구글 3일~2주 / 네이버 1~4주** 정도 걸립니다.

---

## 0. 사이트 쪽 준비는 끝났습니다

제가 미리 해둔 것이라 따로 하실 일이 없습니다. 무엇이 되어 있는지만 적어둡니다.

| 항목 | 상태 |
|---|---|
| 페이지마다 제목·설명문 (검색 결과에 보이는 글) | 완료 |
| `sitemap.xml` — 검색엔진에 페이지 목록을 알려주는 파일 | 완료 |
| `robots.txt` — 수집 허용 설정 | 완료 |
| 구조화 데이터 — 사무소 정보·변호사·업무분야·FAQ를 기계가 읽는 형식으로 | 완료 |
| 공유 카드 이미지 (카톡·페북에 링크 붙일 때 뜨는 그림) | 완료 |
| 주소 통일 설정 (`chaminlaw.com` → `www.chaminlaw.com`) | 파일 준비 완료 |

---

## 1. GitHub Pages 에 올리기

이 PC에 git 이 설치되어 있지 않으므로, **웹브라우저에서 파일을 끌어다 놓는
방식**으로 안내합니다. 명령어를 쓸 일이 없습니다.

### 1-1. 저장소 만들기

1. https://github.com 가입 (이미 계정이 있으면 로그인)
2. 오른쪽 위 `+` → **New repository**
3. Repository name: `chaminlaw` (아무 이름이나 괜찮습니다)
4. **Public** 선택
   - 무료 계정은 Public 이어야 GitHub Pages 를 쓸 수 있습니다.
   - Public 이면 **올린 파일이 누구에게나 보입니다.** 홈페이지 내용은
     어차피 공개될 것이라 상관없지만, 인수인계 문서인 `README.md` 와
     이 파일(`검색등록-안내.md`)도 함께 보입니다. 내부 메모가 남는 게
     꺼려지면 이 두 파일은 올리지 말고 PC에만 두세요. 사이트 동작에는
     아무 영향이 없습니다.
   - 비밀번호나 개인정보는 어느 파일에도 들어 있지 않습니다.
5. **Create repository**

### 1-2. 파일 올리기

1. 만들어진 저장소 화면에서 **uploading an existing file** 링크 클릭
   (또는 `Add file` → `Upload files`)
2. 탐색기에서 `chamin-law` 폴더를 열고, **폴더가 아니라 그 안의
   내용물 전체**를 선택해서 브라우저 창으로 끌어다 놓으세요.
   폴더째 올리면 주소가 `www.chaminlaw.com/chamin-law/` 가 되어버립니다.
3. `.htaccess`, `.nojekyll`, `CNAME` 처럼 점으로 시작하거나 확장자가 없는
   파일도 함께 올라가야 합니다. 탐색기에서 **숨김 항목 표시**를 켜두세요.
4. 아래 **Commit changes** 클릭

`css`, `images`, `js` 폴더는 끌어다 놓으면 하위 파일까지 함께 올라갑니다.
업로드 후 저장소 첫 화면에 `index.html` 이 보이면 제대로 된 것입니다.

### 1-3. Pages 켜기

1. 저장소 상단 **Settings** → 왼쪽 메뉴 **Pages**
2. Source: `Deploy from a branch`
3. Branch: `main` / 폴더 `/ (root)` → **Save**
4. 같은 화면 **Custom domain** 에 `www.chaminlaw.com` 입력 → **Save**
   - 폴더에 `CNAME` 파일을 넣어두었으므로 자동으로 채워져 있을 수도 있습니다
5. DNS 확인이 끝나면(2번 작업 후) **Enforce HTTPS** 체크박스를 켜세요.
   처음에는 회색으로 비활성 상태이며, 인증서 발급에 최대 24시간 걸립니다.

---

## 2. 가비아 DNS 고치기 — **지금 잘못되어 있습니다**

현재 상태를 조회해 보니 두 가지 문제가 있습니다.

| 문제 | 현재 | 되어야 할 값 |
|---|---|---|
| A 레코드 IP 끝자리 누락 | `185.199.108.15` 외 3개 | `185.199.108.153` 외 3개 |
| `www` 레코드 없음 | (존재하지 않음) | CNAME → `<계정명>.github.io` |

### 설정 방법

1. https://dns.gabia.com 접속 → 가비아 계정으로 로그인
2. `chaminlaw.com` → **DNS 설정** 또는 **DNS 관리툴**
3. 아래 표대로 맞춥니다.

| 타입 | 호스트 | 값 | 비고 |
|---|---|---|---|
| A | `@` | `185.199.108.153` | 기존 `.15` 수정 |
| A | `@` | `185.199.109.153` | 기존 `.15` 수정 |
| A | `@` | `185.199.110.153` | 기존 `.15` 수정 |
| A | `@` | `185.199.111.153` | 기존 `.15` 수정 |
| CNAME | `www` | `<본인 GitHub 계정명>.github.io.` | **새로 추가** |

- `<본인 GitHub 계정명>` 은 가입할 때 정한 아이디입니다.
  계정명이 `chaminlaw` 라면 값은 `chaminlaw.github.io.` 입니다.
  저장소 이름이 아니라 **계정명**입니다.
- 가비아는 CNAME 값 끝에 **점(`.`)** 을 붙여야 하는 경우가 있습니다.
  저장이 안 되면 점을 빼고 다시 시도해 보세요.

### 건드리면 안 되는 것

**TXT 레코드 `v=spf1 include:spf.worksmobile.com ~all` 과 MX 레코드는
절대 지우지 마세요.** 네이버웍스 메일 설정입니다. 지우면
`@chaminlaw.com` 메일이 안 옵니다.

### 반영 확인

DNS 변경은 보통 10분~1시간, 길면 하루 걸립니다.
윈도우 명령 프롬프트에서 아래를 입력해 확인할 수 있습니다.

```
nslookup www.chaminlaw.com
nslookup chaminlaw.com
```

`www` 쪽에서 `github.io` 가 보이고, apex 쪽 IP 가 `.153` 으로 끝나면 성공입니다.

---

## 2-1. 주소 통일은 GitHub 이 알아서 합니다

Custom domain 을 `www.chaminlaw.com` 으로 지정하면, `chaminlaw.com` 으로
들어온 접속을 GitHub 이 자동으로 `www` 로 넘겨줍니다. 따로 설정할 것이 없습니다.

**참고:** 폴더에 있는 `.htaccess` 와 `netlify.toml` 은 GitHub Pages 에서는
쓰이지 않습니다. 다른 호스팅으로 옮길 때를 대비해 남겨둔 것이니 그냥 두셔도
되고, 지우셔도 사이트에 영향이 없습니다.

### 최종 확인

아래 네 가지가 **전부** 같은 화면이 뜨고, 주소창이
`https://www.chaminlaw.com` 으로 바뀌면 완료입니다.

- `chaminlaw.com`
- `www.chaminlaw.com`
- `http://chaminlaw.com`
- `http://www.chaminlaw.com`

---

## 3. 네이버 등록 — **가장 중요합니다**

국내 검색은 네이버 비중이 큽니다. 특히 지역 법률사무소는 네이버에서
찾는 경우가 대부분이라 여기부터 하세요.

### 3-1. 네이버 서치어드바이저 (웹사이트 등록)

1. https://searchadvisor.naver.com 접속 → 네이버 계정으로 로그인
2. **웹마스터 도구** → 사이트 등록
3. `https://www.chaminlaw.com` 입력
4. **사이트 소유확인** 화면이 나옵니다. `HTML 태그` 방식을 선택하면
   아래 같은 줄을 줍니다.

   ```html
   <meta name="naver-site-verification" content="a1b2c3d4...">
   ```

5. 이 줄을 복사해서 `index.html` 파일을 열고, 위쪽에 있는
   **"검색엔진 소유확인 태그 자리"** 주석을 찾아 그 안에 붙여넣으세요.
   주석 기호(`<!--` 와 `-->`)를 지워야 실제로 동작합니다.
6. 수정한 `index.html` 을 다시 업로드한 뒤, 네이버 화면에서 **소유확인** 클릭
7. 확인되면 → **요청 → 사이트맵 제출** → `sitemap.xml` 입력
8. **요청 → 웹페이지 수집**에 주요 페이지 주소를 하나씩 넣어 수집 요청

### 3-2. 네이버 스마트플레이스 (지도 등록) — 놓치지 마세요

"군산 변호사" 로 검색했을 때 지도와 함께 위에 뜨는 자리입니다.
웹사이트 등록보다 이쪽 효과가 더 클 수 있습니다.

1. https://smartplace.naver.com 접속
2. 업체 등록 → 업종 "법률사무소"
3. 상호 `법률사무소 차민`, 주소 `전북특별자치도 군산시 미장안7길 6, 3층`,
   전화 `063-452-9075` 입력
4. **홈페이지 항목에 `https://www.chaminlaw.com` 입력**
5. 사업자등록증 등 서류로 업체 인증 (심사에 며칠 걸립니다)
6. 등록이 끝나면 `location.html` 의 지도 자리에 넣을
   **지도 퍼가기 코드**도 받을 수 있습니다 (README 4번 참고)

---

## 4. 구글 등록

1. https://search.google.com/search-console 접속 → 구글 계정 로그인
2. 속성 추가 → **URL 접두어** 선택 → `https://www.chaminlaw.com` 입력
3. 소유확인 → **HTML 태그** 방식 선택 → 받은 줄을 3-1과 같은 자리에 붙여넣기
   (네이버 태그와 나란히 두면 됩니다)
4. `index.html` 재업로드 후 **확인** 클릭
5. 왼쪽 메뉴 **Sitemaps** → `sitemap.xml` 입력 후 제출
6. 상단 검색창에 `https://www.chaminlaw.com` 입력 → **색인 생성 요청**

### 구글 비즈니스 프로필도 함께

https://business.google.com 에서 같은 정보로 업체를 등록하면
구글 지도와 검색에 사무소가 나타납니다. 네이버 스마트플레이스와 별개입니다.

---

## 5. 다음(카카오) 등록

1. https://register.search.daum.net/index.daum 접속
2. 사이트 주소 `https://www.chaminlaw.com` 입력 후 신청
3. 카카오맵 장소 등록: https://map.kakao.com 에서 장소 제안

---

## 6. 등록 후 확인하는 법

며칠 뒤 각 검색창에 아래처럼 입력해 보세요.

- 구글: `site:www.chaminlaw.com`
- 네이버: `chaminlaw.com`

페이지 목록이 나오면 수집이 된 것입니다. 아무것도 안 나오면 아직 처리 중이거나
소유확인이 안 끝난 것이니 서치콘솔·서치어드바이저 화면에서 상태를 확인하세요.

### 점검 도구

| 확인할 것 | 주소 |
|---|---|
| 구조화 데이터가 제대로 읽히는지 | https://search.google.com/test/rich-results |
| 카톡·페북 공유 카드가 잘 뜨는지 | https://developers.facebook.com/tools/debug/ |
| 모바일에서 문제없는지 / 속도 | https://pagespeed.web.dev |

---

## 7. 검색에 잘 걸리게 하려면 (등록 이후)

기술 설정은 끝났고, 이제부터는 내용 문제입니다.

1. **변호사 학력·경력을 채우세요.** 지금 `확인 후 반영 예정입니다.` 로
   비어 있는 부분입니다. 검색엔진은 내용이 얕은 페이지를 낮게 봅니다.
   무엇보다 의뢰인이 가장 궁금해하는 정보입니다.
2. **네이버 스마트플레이스 정보를 채우세요.** 사진, 영업시간, 소개글.
   지역 검색은 여기가 거의 전부입니다.
3. **사건 사례나 법률 정보 글을 올릴 계획이라면** 별도 게시판 페이지를
   만드는 것이 좋습니다. 지금은 정적 사이트라 글을 쓰려면 HTML을 직접
   고쳐야 하니, 글을 꾸준히 올리실 생각이면 그때 말씀해 주세요.
4. **광고성 문구를 넣지 마세요.** 변호사 광고는 규정 제한이 있고,
   "승소율 1위" 같은 표현은 검색엔진에도 좋지 않습니다.

---

## 자주 막히는 지점

**Q. GitHub Pages 주소로는 되는데 chaminlaw.com 은 안 됩니다**
DNS 가 아직 반영 안 됐거나 A 레코드 `.153` 수정이 빠진 것입니다.
2번의 `nslookup` 으로 확인하세요.

**Q. Enforce HTTPS 체크박스가 회색이라 눌리지 않습니다**
DNS 가 GitHub 을 정확히 가리켜야 인증서를 발급합니다. DNS 를 고친 뒤
최대 24시간 기다리면 활성화됩니다. Settings > Pages 에서 Custom domain 을
지웠다가 다시 입력하면 재시도됩니다.

**Q. 사이트는 뜨는데 글꼴이 깨지고 배치가 이상합니다**
`css` 폴더가 안 올라간 것입니다. 저장소에 `css/style.css` 가 있는지
확인하세요.

**Q. `.htaccess` 를 올렸는데 사이트가 500 오류가 납니다**
GitHub Pages 에서는 이 파일이 무시되므로 해당 없습니다. 나중에 카페24 등
일반 웹호스팅으로 옮겼을 때 이 증상이 나면, 파일 이름을 `htaccess.txt` 로
바꿔 비활성화하고 주소 통일은 호스팅 관리자 페이지에서 처리하세요.

**Q. 소유확인이 계속 실패합니다**
`index.html` 을 고친 뒤 다시 업로드했는지, 주석 기호를 지웠는지
확인하세요. 브라우저에서 `www.chaminlaw.com` 접속 후 마우스 오른쪽 →
"페이지 소스 보기" 에서 태그가 실제로 보여야 합니다.

**Q. 네이버에 등록했는데 몇 주째 안 나옵니다**
네이버는 구글보다 느리고, 새 사이트는 더 걸립니다. 서치어드바이저의
"웹페이지 수집" 에서 페이지별로 수집 요청을 넣어보세요.
스마트플레이스 등록이 되어 있으면 그쪽이 먼저 노출됩니다.
